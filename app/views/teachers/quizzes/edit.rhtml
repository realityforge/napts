<h1>Editing quiz</h1>

<%= start_form_tag( :action => 'update', :id => @quiz ) %>
  <%= render( :partial => 'form' ) %>
  <%= submit_tag( 'Submit' ) %>
<%= end_form_tag %>

<%= link_to( 'Back', :action => 'show', :id => @quiz ) %>

