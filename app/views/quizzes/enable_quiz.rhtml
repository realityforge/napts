<table>
  <tr>
    <th>Subject ID</th>
    <th>Name</th>
    <th>Description</th>
    <th>Duration</th>
    <th></th>
  </tr>
  <% for quiz in @quizzes %>
    <tr>
      <td><%= quiz.subject.code %></td>
      <td><%= quiz.name %></td>
      <td><%= quiz.description %></td>
      <td><%= quiz.duration %></td>
      <% if quiz.enable %>
        <td><%= link_to( 'Disable Test', :action => 'enable', :id => quiz.id ) %></td>
      <% else %>
        <td><%= link_to( 'Enable Test', :action => 'enable', :id => quiz.id ) %></td>
      <% end %>
    </tr>
  <% end %>
</table>
